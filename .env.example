# Colab FastAPI backend exposed via ngrok (no trailing slash)
# Local: copy to .env.local
# Vercel: Project Settings → Environment Variables → add NGROK_API_URL for Production/Preview/Development
NGROK_API_URL=https://indigenous-noncontiguously-angelo.ngrok-free.dev
